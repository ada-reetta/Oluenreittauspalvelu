SELECT Account.id, Account.name FROM Beer
    LEFT JOIN Task ON Task.account_id = Account.id
    WHERE (Task.done IS null OR Task.done = 1)
    GROUP BY Account.id
    HAVING COUNT(Task.id) = 0

#kuinka monta arvostelua kullakin oluella on
SELECT Beer.name, COUNT(Rating.id) FROM Beer
    LEFT JOIN Rating ON Rating.beer_id = Beer.id
    GROUP BY Beer.id

#mikä on kunkin oluen arvostelujen keskiarvo
SELECT Beer.name, AVG(Rating.rating) FROM Beer
    LEFT JOIN Rating ON Rating.beer_id = Beer.id
    GROUP BY Beer.id

b = Beer.query.get(beer_id)